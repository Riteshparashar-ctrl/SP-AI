# SP-AI
A secure AI chat web app powered by Google Gemini 3.6 Flash (Interactions API). Built with Node.js + Express backend, vanilla HTML/CSS/JS frontend, and local JSON storage. Features user signup/login, per-user persistent chat history, typing indicator, and a dark purple UI. API key stays server-side only.
